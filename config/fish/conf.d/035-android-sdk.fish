set -Ux ANDROID_SDK_ROOT /opt/android-sdk
set -Ux PATH $ANDROID_SDK_ROOT/emulator $PATH
set -Ux PATH $ANDROID_SDK_ROOT/platform-tools $PATH
set -Ux PATH $ANDROID_SDK_ROOT/tools/bin $PATH
set -Ux PATH $ANDROID_SDK_ROOT/tools $PATH